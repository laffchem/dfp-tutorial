services:
  web:
    build: .
    ports:
      - "8000:8000"
    # Use the container Python to invoke manage.py so we run the project's code reliably
    command: uv run manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    environment:
      - DB_HOST=db
    depends_on:
      - db

  db:
    image: postgres:18
    volumes:
      - postgres_data:/var/lib/postgresql
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  postgres_data: